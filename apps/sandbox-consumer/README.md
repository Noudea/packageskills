# Sandbox Consumer

Consumer app for testing the generated `sandbox-skills` command from `apps/sandbox-maintainer`.

Quick flow:

1. `pnpm install`
2. `pnpm exec sandbox-skills install`
3. Inspect `.claude/skills`, `.opencode/skills`, and `.codex/skills`
4. `pnpm exec sandbox-skills remove`
